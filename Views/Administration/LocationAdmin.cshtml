@using Microsoft.AspNetCore.Http;
@model HUS_project.Models.EditStorageLocationModel
@inject IHttpContextAccessor httpContextAccessor;
@{
    ViewData["Title"] = "Placerings Administration";
}
<form Asp-action="LocationAdmin" method="post">
    <div class="container-fluid blue-oyster-bar">
        <div class="row g-2">
            <div class="col-md-1">
                <div class="col-md">
                </div>
            </div>
            <div class="col-md">
                <div class="form-floating">
                    <datalist class="form-select" id="floatingSelectGrid" aria-label="Floating label select example" asp-for="@Model.Buildings">
                        <option selected>Open this select menu</option>
                        @foreach (var item in Model.Buildings)
                        {
                            <option>@item</option>
                        }
                    </datalist>
                    <label for="floatingSelectGrid">Bygning</label>
                </div>
            </div>
            <div class="col-md">
                <div class="form-floating">
                    <select class="form-select" id="floatingSelectGrid" aria-label="Floating label select example" asp-for="@Model.RoomNumbers">
                        <option selected>Open this select menu</option>
                        @foreach (var item in Model.RoomNumbers)
                        {
                            <option>@item</option>
                        }
                    </select>
                    <label for="floatingSelectGrid">Bygning</label>
                </div>
            </div>
            <div class="col-md-1">
                <div class="col-md">
                    <label class="text-white my-1">
                        Lokale: <input list="roomNr" placeholder="Vælg" />
                    </label>
                </div>
            </div>
            <div class="col-md-1">
                <div class="col-md">
                    <label class="text-white my-1">
                        Lokale: <input list="roomNr" placeholder="Vælg" />
                    </label>
                </div>
            </div>
            <div class="col-md-1">
                <div class="col-md">
                    <label class="text-white my-1">
                        Lokale: <input list="roomNr" placeholder="Vælg" />
                    </label>
                </div>
            </div>
            <div class="col-md-3">
                <div class="col-md">
                    <label class="text-white my-1">
                        Lokale: <input list="roomNr" placeholder="Vælg" />
                    </label>
                </div>
            </div>
            <div class="col-md-2">
                <div class="col-md">
                    <label class="text-white my-1">
                        Lokale: <input list="roomNr" placeholder="Vælg" />
                    </label>
                </div>
            </div>
            <div class="col-md-1">
                <div class="col-md">
                    <label class="text-white ">
                        Lokale: <input list="roomNr" placeholder="Vælg" />
                    </label>
                </div>
            </div>


        </div>
    </div>
    <div class="container-md rounded-top rounded-3 border border-3" >
        this is a test header
    </div>
    <div class="container-md border border-3">

    </div>
    <div class="container-md rounded-bottom rounded-3 border border-3">

    </div>

</form>

